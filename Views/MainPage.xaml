<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="bioscoop_app.MainPage">

    <Grid Padding="30,0">

        <StackLayout
            x:Name="LoginView"
            Spacing="20"
            VerticalOptions="Center"
            HorizontalOptions="Center">
            <Image
                Source="logo.png"
                HeightRequest="150"
                Aspect="AspectFit"
                HorizontalOptions="Center"
                SemanticProperties.Description="App logo" />
            <Label
                Text="Welkom bij Bioscoop"
                FontSize="24"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                SemanticProperties.HeadingLevel="Level1" />
            <Label
                Text="Log in om je volgende film te boeken"
                FontSize="14"
                HorizontalOptions="Center"
                SemanticProperties.HeadingLevel="Level2" />
            <Button
                x:Name="LoginBtn"
                Text="Inloggen"
                SemanticProperties.Hint="Klik om in te loggen"
                Clicked="OnLoginClicked"
                HorizontalOptions="Center" />
        </StackLayout>

        <ScrollView x:Name="HomeView" IsVisible="False">
            <StackLayout
                Spacing="25">
                <Image
                    Source="dotnet_bot.png"
                    HeightRequest="185"
                    Aspect="AspectFit"
                    SemanticProperties.Description="dot net bot in a submarine number ten" />

                <Label
                    Text="Hello, World!"
                    Style="{StaticResource Headline}"
                    SemanticProperties.HeadingLevel="Level1" />

                <Label
                    Text="Welcome to &#10;.NET Multi-platform App UI"
                    Style="{StaticResource SubHeadline}"
                    SemanticProperties.HeadingLevel="Level2"
                    SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

                <Button
                    x:Name="CounterBtn"
                    Text="Click me"
                    SemanticProperties.Hint="Counts the number of times you click"
                    Clicked="OnCounterClicked"
                    HorizontalOptions="Fill" />

                <Image
                    x:Name="UserPictureImg"
                    SemanticProperties.Description="User's picture"
                    HeightRequest="200"
                    HorizontalOptions="Center" />

                <Label
                    x:Name="UsernameLbl"
                    Text=""
                    SemanticProperties.HeadingLevel="Level2"
                    SemanticProperties.Description="User's name"
                    FontSize="18"
                    HorizontalOptions="Center" />

                <Button
                    x:Name="LogoutBtn"
                    Text="Log Out"
                    SemanticProperties.Hint="Click to log out"
                    Clicked="OnLogoutClicked"
                    HorizontalOptions="Center" />
            </StackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
