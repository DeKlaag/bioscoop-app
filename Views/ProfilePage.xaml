<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="bioscoop_app.Views.ProfilePage"
             Shell.TabBarIsVisible="False"
             Title="Profile"> 
    <Shell.TitleView>
        <ContentView />
    </Shell.TitleView>
    <ContentPage.Content>
        <VerticalStackLayout 
            Padding="30,0"
            Spacing="25">

            <StackLayout >
                <Image
                    x:Name="UserPictureImg"
                    SemanticProperties.Description="User's picture"
                    HeightRequest="200"
                    HorizontalOptions="Center" />

                <Label
                    x:Name="UsernameLbl"
                    Text=""
                    SemanticProperties.HeadingLevel="Level2"
                    SemanticProperties.Description="User's name"
                    FontSize="18"
                    HorizontalOptions="Center" />

                <Button
                    Text="Check-in scanner"
                    SemanticProperties.Hint="Scan reservering-QR-codes om in te checken"
                    Clicked="OnCheckInScannerClicked"
                    HorizontalOptions="Center" />

                <Button
                    Text="Meldingen"
                    SemanticProperties.Hint="Beheer je meldingsvoorkeuren"
                    Clicked="OnNotificationPreferencesClicked"
                    HorizontalOptions="Center" />

                <Button
                    x:Name="LogoutBtn"
                    Text="Log Out"
                    SemanticProperties.Hint="Click to log out"
                    Clicked="OnLogoutClicked"
                    HorizontalOptions="Center" />

            </StackLayout>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>